import  Card  from "@mui/material/Card";
import Typography  from "@mui/material/Typography";

export default function ServerIsBusy(){
      <Card style={{width:'50vw',height:'50vh', margin:'0 auto', marginTop:'5rem',display:'flex', flexDirection:'column', justifyContent:'center',alignItems:'center'}}>
            <Typography variant="h6"  color={"darkred"}>
                Sorry for inconvenience.Looks like we are not able to establish connection to the server at this moment! Please try again later.
            </Typography>
      </Card>
}